name: Code Analysis
on:
  push:
    branches: [main]
jobs:
  code-analysis:
    permissions:
      actions: read
      security-events: write
    uses: ./.github/workflows/node.yml
    with:
      suppression: true
    secrets: 
      token: ${{ secrets.SNYK_TOKEN }}